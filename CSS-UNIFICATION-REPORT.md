# تقرير توحيد CSS — 2026-09-25

> **الحالة: جاهز للمراجعة ⏸️ — في انتظار كلمة «انشر»**
>
> لم يتم النشر ولم يتم الدمج على `master`. كل العمل على الفرع:
> `arena/01a0da28-nasr-realestate-github-io`
>
> **ملاحظة عن اسم الفرع:** المهمة طلبت فرعًا باسم `refactor/unify-css-2026-09-25`،
> لكن جلسة العمل هذه مرتبطة تقنيًا بالفرع `arena/01a0da28-nasr-realestate-github-io`
> ولا يمكن العمل على فرع آخر. المحتوى وال commits جاهزة كما لو كانت على الفرع المطلوب.

---

## 1️⃣ الجرد (Inventory)

### كتل `<style>` — كانت 19 كتلة (الآن: صفر)

| # | الملف | أسطر CSS | الحالة |
|---|-------|----------:|--------|
| 1 | `assets/css/style.css` (الملف الرئيسي) | 665 | أُعيد بناؤه موحدًا |
| 2 | `_layouts/default.html` (زر الوكيل العائم) | 216 | نُقل للقسم المشترك |
| 3 | `_layouts/app.html` (أساس صفحات التطبيق) | 38 | نُقل مُنطَّقًا `layout-app` |
| 4 | `_layouts/property_page.html` | 575 | نُقل مُنطَّقًا `layout-property_page` |
| 5 | `_layouts/request_page.html` | 534 | نُقل مُنطَّقًا `layout-request_page` |
| 6 | `_includes/floating-menu.html` | 168 | نُقل للقسم المشترك |
| 7 | `_includes/footer.html` | 138 | نُقل للقسم المشترك |
| 8 | `index.html` | 721 | نُقل مُنطَّقًا `homepage` |
| 9 | `agent.html` | 991 | نُقل مُنطَّقًا `page-agent` |
| 10 | `add-your-property.html` | 423 | نُقل مُنطَّقًا `page-add` |
| 11 | `properties.html` | 472 | نُقل مُنطَّقًا `page-properties` |
| 12 | `property-images.html` | 361 | نُقل مُنطَّقًا `page-pgimages` |
| 13 | `requests.html` | 59 | نُقل مُنطَّقًا `page-requests` |
| 14 | `about-us.html` | 385 | نُقل مُنطَّقًا `page-about` |
| 15 | `contact-us.html` | 436 | نُقل مُنطَّقًا `page-contact` |
| 16 | `privacy-policy.html` | 284 | نُقل مُنطَّقًا `page-privacy` |
| 17 | `404.html` | 184 | نُقل مُنطَّقًا `page-404` |
| 18 | `luxury-gallery.html` (مستقلة) | 300 | نُقل مُنطَّقًا `page-luxury` |
| 19 | `assets/tools/valuation.html` (مستقلة) | 322 | نُقل مُنطَّقًا `page-valuation` |
| 20 | `tools/generator.html` (مستقلة) | 429 | نُقل مُنطَّقًا `page-generator` |

### الأنماط السطرية `style="..."` — كانت 113 (الآن: 17 ديناميكية فقط)

| الموقع | العدد | الإجراء |
|---|---:|---|
| `_includes/footer.html` | 31 | → أصناف `ft-*` جديدة |
| `agent.html` | 21 | 18 → أصناف `ag-*`، 3 ديناميكية بقيت |
| `_properties/*.md` (7 ملفات) | 16 | → أصناف `u-*` / `opt-*` |
| `add-your-property.html` | 12 | 11 → أصناف `u-*`، 1 ديناميكية بقيت |
| `tools/generator.html` | 7 | 4 → أصناف `gen-*`، 3 ديناميكية بقيت |
| `index.html` | 6 | → أصناف `img-ph` / `u-*` |
| `_includes/property_card.html` | 6 | **بقيت** (Liquid ديناميكي `{{ main_color }}` — مستحيل نقله لملف ثابت) |
| `properties.html` | 3 | 2 → أصناف، 1 ديناميكية بقيت |
| `property-images.html` | 2 | **بقيت** (تُبدَّل عبر JS) |
| `contact-us.html` / `about-us.html` | 2+2 | → أصناف `u-*` |
| `_layouts/property_page.html` | 2 | → أصناف `img-ph` |
| `requests.html` / `luxury-gallery.html` / `valuation.html` | 1+1+1 | الطلبات ديناميكية بقيت، الباقي → أصناف |

**الباقي (17):** 6 بـ Liquid + 11 بـ `display:none` تُدار عبر JavaScript —
إبقاؤها هو القرار الصحيح (نقلها كان سيكسر التفاعلية أو يستحيل تقنيًا).

---

## 2️⃣ التحليل (Analysis)

- **إجمالي القواعد قبل التوحيد:** ~1546 قاعدة في 20 مصدرًا.
- **متغيرات `:root` متضاربة:** 22 متغيرًا بنفس الاسم وقيم مختلفة
  (مثال: `--color-surface-2` = `#141414` في القاعدة مقابل `#1a1a1a` في `app.html`؛
  `--bg`/`--gold`/`--border` بقيم مختلفة بين الصفحات المستقلة).
  **الحل:** المتغيرات العامة بقيت في `:root`، ومتغيرات كل صفحة نُطِّقت
  (`html.page-x, body.page-x`) — لا تضارب بعد اليوم.
- **محددات متكررة في ملفين+:** 30 محددًا — عُولجت كلها
  (نطق / حذف الميت / توحيد المتطابق / حذف نسخة القاعدة المحكومة بنسخة الصفحة).
- **أصناف ميتة (Dead CSS):** 187 صنفًا — بعد استبعاد الإيجابيات الكاذبة
  (أصناف تُنشأ عبر `className=` في JS مثل `.msg-bot` و`.pg-card` و`.slide`)،
  **حُذفت 292 قاعدة ميتة مؤكدة** من القاعدة العامة (أنماط صفحات قديمة:
  about/success/slider/smart-search/error/listing/team/teaser/filters…).
- **مكونات خاملة مقصودة (أُبقيت):** أنماط `.navbar-*` و`.property-card-*`
  — ملفات الـ includes موجودة وسليمة وقد تُفعَّل مجددًا.
- **حركات `@keyframes`:** 34 تعريفًا → **23 فريدة**
  (حُذفت 7 يتيمة بلا أي استخدام، ودُمج `fadeIn`×2 و`spin`×3،
  وأُعيدت تسمية `cardIn` المتضاربة → `agentCardIn` + `pgCardIn`).

---

## 3️⃣ ما تم تنفيذه (Unification)

### الملف الموحد `assets/css/style.css` — 6876 سطرًا بالهيكل المطلوب

```
1. RESET & BASE          — إعادة الضبط + body/html/img/main
2. CSS VARIABLES         — :root العامة (21 متغيرًا)
3. TYPOGRAPHY            — النصوص الذهبية
4. LAYOUT                — الشريط العلوي + التذييل
5. COMPONENTS            — المكونات + 4 قواعد مُوحدة + 4 مشتركة + 15 قسم صفحة مُنطَّق
6. UTILITIES             — 58 صنفًا بديلًا عن style="..." الثابتة
7. RESPONSIVE            — 43 استعلام @media بنفس ترتيب التحميل الأصلي
8. ANIMATIONS            — 23 حركة @keyframes بلا تكرار
```

### ضمانات عدم التغيير البصري (100%)

1. **النطق الكامل:** كل قسم صفحة مسبوق بـ `body.page-x`
   (مثال: `body.page-agent .msg-user`) — مستحيل أن يتسرب لصفحة أخرى.
2. **ترتيب الطبقات محفوظ:** القاعدة ← المشترك ← الصفحات ← media —
   نفس ترتيب الفوز الأصلي.
3. **تحييد الصفحات الخمس الجديدة على القاعدة:**
   صفحات `agent` و`add-your-property` و`luxury` و`valuation` و`generator`
   لم تكن تحمّل القاعدة العامة أصلًا، فأُضيفت لها قواعد تحييد صريحة
   (`display:block`، `line-height:normal`، `scroll-behavior:auto`…)
   تعيد قيمها الأصلية حرفيًا.
4. **النسخة الاحتياطية:** `assets/css/style.css.orig`
   (مستثناة من النشر في `_config.yml`).

### ملفات HTML المعدلة (26 ملفًا)

- حُذفت كل كتل `<style>` الـ 19.
- حُوِّلت 96 سمة `style` ثابتة لأصناف.
- أُضيف `body_class` لكل صفحة + أصناف `layout-*` تلقائية على `<html>` و`<body>`.
- أُضيفت وصلة `style.css` إلى `app.html` والصفحات المستقلة الثلاث.

---

## 4️⃣ التحقق الآلي (كلها ناجحة ✅)

- توازن الأقواس `{}` — ✅ (1388/1388)
- لا `<style>` متبقٍ، لا Liquid داخل CSS، لا `@import` — ✅
- كل `var(--x)` يُحلّ (عام أو نطاق الصفحة) — ✅
- كل `animation-name` يُحلّ في `@keyframes` — ✅
- لا تصادم بين media القاعدة وأقسام الصفحات (باستثناء موثّق واحد بلا تصادم خصائص) — ✅
- كل الأصناف الجديدة معرّفة ومستخدمة — ✅

---

## 5️⃣ خطة النشر (بعد كلمة «انشر»)

1. مراجعة أخيرة لـ `git diff` + دمج الفرع في `master` + دفع.
2. مراقبة بناء GitHub Pages.
3. **التراجع عند الحاجة:** الملف الأصلي محفوظ في
   `assets/css/style.css.orig` + سجل git — الاسترجاع فوري.

## 6️⃣ ملاحظة صراحة عن الأداء

حجم `style.css` أصبح ~230KB (~36KB بعد الضغط) لأنه يضم كل الموقع،
مقابل ~52KB قبلًا. في المقابل حُذف ~150KB من CSS مضمّن غير قابل للتخزين المؤقت
من كل صفحة. الزيارات المتكررة أخف بكثير (ملف واحد يُحفظ في الكاش)،
والزيارة الأولى أثقل قليلًا في الصفحات الصغيرة. إن رغبت لاحقًا يمكن تقسيم
الملف (قاعدة + ملف لكل صفحة ثقيلة) — البنية الحالية تجعل ذلك سهلًا.
