# =========================================================
# === ⚙️ إعدادات الموقع النهائية (GitHub Pages) ===
# =========================================================

# 1. المعلومات الأساسية
title: "سمسار طلبك | شقق للبيع في مدينة نصر"
email: "elraikonly@outlook.sa"
description: "بوابتك العقارية الأولى في مدينة نصر. تصفح أحدث الشقق والمحلات والمكاتب التجارية للبيع والإيجار."
url: "https://nasr-realestate.github.io"
baseurl: ""
lang: "ar"
timezone: Africa/Cairo

# 2. بيانات المالك (Schema)
author:
  name: "طارق طنطاوي"
  phone: "+201147758857"
  email: "elraikonly@outlook.sa"
logo: "/assets/img/logo.webp"

# 3. إعدادات البناء والروابط
markdown: kramdown
permalink: pretty

# 4. الإضافات (عضلات الموقع)
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

# 5. تحليلات جوجل (تم التفعيل)
# هذا السطر هو الذي سيشغل التحليلات في كل صفحات الموقع تلقائياً
google_analytics: "G-8NBQRQ6N10"

# 6. 🔥 الروابط الذكية (Collections)
collections:
  properties:
    output: true
    permalink: /properties/:title/
  requests:
    output: true
    permalink: /requests/:title/

# 7. الإعدادات الافتراضية
defaults:
  - scope:
      path: ""
    values:
      layout: "default"
      image: "/assets/img/social-home.webp"
  
  # تخصيص العقارات
  - scope:
      path: ""
      type: "properties"
    values:
      layout: "property_page"
  
  # تخصيص الطلبات
  - scope:
      path: ""
      type: "requests"
    values:
      layout: "request_page"

# 8. الملفات المستبعدة
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - README.md
  - vendor
