# =========================================================
# === ⚙️ إعدادات الموقع النهائية (GitHub Pages) ===
# =========================================================

# 1. المعلومات الأساسية
title: "سمسار طلبك | شقق للبيع في مدينة نصر"
email: "elraikonly@outlook.sa"
description: "بوابتك العقارية الأولى في مدينة نصر. تصفح أحدث الشقق والمحلات والمكاتب التجارية للبيع والإيجار."
url: "https://nasr-realestate.github.io"
baseurl: ""
lang: "ar"
timezone: Africa/Cairo

# 2. بيانات المالك (Schema)
author:
  name: "طارق طنطاوي"
  phone: "+201147758857"
  email: "elraikonly@outlook.sa"
logo: "/assets/img/logo.webp"

# 3. إعدادات البناء والروابط
markdown: kramdown
permalink: pretty

# 4. الإضافات (عضلات الموقع)
plugins:
  - jekyll-seo-tag
  # 🔥 معطل: - jekyll-sitemap  # لأننا سنستخدم sitemap يدوي

# 5. تحليلات جوجل (تم التفعيل)
google_analytics: "G-8NBQRQ6N10"

# 6. 🔥 روابط العقارات بالإنجليزية (باستخدام basename) 🔥
collections:
  properties:
    output: true
    permalink: /property/:basename/  # 🔥 يستخدم اسم الملف الإنجليزي
  requests:
    output: true
    permalink: /request/:basename/   # 🔥 يستخدم اسم الملف الإنجليزي

# 7. الإعدادات الافتراضية
defaults:
  - scope:
      path: ""
    values:
      layout: "default"
      image: "/assets/img/social-home.webp"
      language: "ar"
  
  # تخصيص العقارات
  - scope:
      path: ""
      type: "properties"
    values:
      layout: "property_page"
      business_type: "RealEstateAgent"
      price_currency: "EGP"
  
  # تخصيص الطلبات
  - scope:
      path: ""
      type: "requests"
    values:
      layout: "request_page"

# 8. الملفات المستبعدة
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - README.md
  - vendor
  - generate_sitemap.py  # 🔥 مستبعد
