# =========================================================
# === ⚙️ إعدادات الموقع المحسنة لـ SEO والمكالمات ===
# =========================================================

# 1. المعلومات الأساسية (محسنة للـ SEO)
title: "شقق للبيع في مدينة نصر | سمسار طلبك"
email: "elraikonly@outlook.sa"
description: "⭐ تبحث عن شقق للبيع في مدينة نصر؟ تصفح أحدث عروض الشقق في جميع مناطق مدينة نصر. تواصل مع خبيرنا طارق طنطاوي مباشرة: 01147758857"
url: "https://nasr-realestate.github.io"
baseurl: ""
lang: "ar"
timezone: Africa/Cairo

# 2. بيانات المالك (محسنة للـ Schema Markup)
author:
  name: "طارق طنطاوي"
  phone: "+201147758857"
  email: "elraikonly@outlook.sa"
logo: "/assets/img/logo.webp"

# 3. إعدادات البناء (Markdown)
markdown: kramdown
permalink: pretty

# 4. الإضافات (GitHub يدعم هذه الإضافات تلقائياً)
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

# 5. Google Analytics (جديد - لتتبع المكالمات)
google_analytics: UA-XXXXXXXXX-X # ⚠️ استبدل برقم التتبع الخاص بك

# 6. 🔥 المجموعات (Collections) - محسن لـ SEO
collections:
  properties:
    output: true
    permalink: /شقق-للبيع-في-مدينة-نصر/:title/ # ⚡ رابط صديق لمحركات البحث
  requests:
    output: true
    permalink: /طلبات-عقارية/:title/

# 7. الإعدادات الافتراضية (محدثة)
defaults:
  # الإعدادات العامة
  - scope:
      path: ""
    values:
      layout: "default"
      image: "/assets/img/social-home.webp"
      phone_primary: "+201147758857"
      phone_secondary: "+201098765432"
  
  # إعدادات العقارات
  - scope:
      path: ""
      type: "properties"
    values:
      layout: "property_page"
      category: "للبيع"
      property_type: "شقة"
  
  # إعدادات الطلبات
  - scope:
      path: ""
      type: "requests"
    values:
      layout: "request_page"
      category: "مطلوب"

# 8. الإعدادات المخصصة (جديدة)
sitemap:
  priority: 0.7
  changefreq: weekly

# 9. التصاريح (للروابط الخارجية)
permissions_policy:
  camera: "()"
  microphone: "()"
  geolocation: "()"

# 10. الملفات المستبعدة
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - README.md
  - vendor
  - .gitignore
  - .DS_Store

# 11. الإعدادات الإضافية (جديدة)
future: false
safe: true
lsi: false
incremental: false
highlighter: rouge

# 12. ضغط HTML (لتحسين السرعة)
compress_html:
  clippings: all
  comments: all
  endings: all
  startings: []
  blanklines: false
  profile: false
