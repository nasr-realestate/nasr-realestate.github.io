name: إرسال إشعار عند إضافة عقار أو طلب جديد

on:
  push:
    branches:
      - master # مراقبة الفرع النشط حالياً لموقعك
    paths:
      - '_properties/**'
      - '_requests/**'
  workflow_dispatch:

jobs:
  send-notification:
    runs-on: ubuntu-latest
    
    steps:
      - name: استنساخ المستودع
        uses: actions/checkout@v4
        with:
          fetch-depth: 2 # قراءة آخر حركات الرفع بدقة

      - name: إعداد Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: تثبيت المكتبات
        run: npm install firebase-admin frontmatter

      - name: إرسال الإشعارات
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          node -e '
          const admin = require("firebase-admin");
          const fs = require("fs");
          const { execSync } = require("child_process");
          const frontmatter = require("frontmatter");
          
          const serviceAccount = JSON.parse(process.env.FIREBASE_SERVICE_ACCOUNT);
          admin.initializeApp({
            credential: admin.credential.cert(serviceAccount)
          });
          
          const db = admin.firestore();
          
          async function sendNotification() {
            try {
              // استراتيجية الاقتناص المباشر لأحدث ملف تم التعامل معه وتفادي خداع الـ Diff
              const output = execSync("git log -1 --name-only --pretty=", { encoding: "utf8" });
              const files = output.split("\n").filter(f =>
                (f.startsWith("_properties/") || f.startsWith("_requests/")) && f.endsWith(".md") && fs.existsSync(f)
              );

              if (files.length === 0) {
                console.log("⚠️ لم يتم العثور على ملفات جديدة في مجلدات العقارات والطلبات.");
                return;
              }

              const tokensSnapshot = await db.collection("fcmTokens").get();
              const tokens = [];
              tokensSnapshot.forEach(doc => tokens.push(doc.id));
              
              if (tokens.length === 0) {
                console.log("❌ لا يوجد مستخدمين مسجلين (Tokens) في سحابة Firestore.");
                return;
              }

              for (const filePath of files) {
                const content = fs.readFileSync(filePath, "utf8");
                const { data } = frontmatter(content);
                
                const isRequest = filePath.startsWith("_requests/");
                const title = data.title || (isRequest ? "طلب جديد" : "عقار جديد");
                const price = data.price || data.budget || "";
                const location = data.location || "";
                const slug = data.slug || "";
                
                const baseUrl = "https://nasr-realestate.github.io";
                const section = isRequest ? "طلبات-عقارية-مستعجلة" : "عقارات-مدينة-نصر";
                const propertyUrl = slug ? `${baseUrl}/${section}/${slug}/` : baseUrl;

                const message = {
                  data: {
                    title: isRequest ? `🔔 طلب جديد: ${title}` : `🏠 عرض جديد: ${title}`,
                    body: `${price ? "💰 " + price : ""} ${location ? "📍 " + location : ""}`.trim() || "اضغط للتفاصيل",
                    url: propertyUrl
                  },
                  tokens: tokens
                };
                
                const response = await admin.messaging().sendEachForMulticast(message);
                console.log(`✅ تم اقتناص الملف بنجاح وإرسال الإشعار لهاتف طارق. نجاح: ${response.successCount}`);
              }
            } catch (error) {
              console.error("❌ حدث خطأ في خادم الإرسال:", error);
            }
          }
          
          sendNotification();
          '
          
