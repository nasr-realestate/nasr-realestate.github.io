# تدقيق تجربة العميل — سمسار طلبك

## نطاق التدقيق

تم التعامل مع النظام كعميل مصري، وليس كاختبار تقني فقط. تم فحص صفحة `agent.html`، عقد الاتصال بالـ Worker، وبداية المسارات الأربعة، كما تم إرسال طلبات فعلية إلى Worker `royal-snow-ea32.footcai-555.workers.dev` بنفس ترويسات المتصفح.

## ما تم إصلاحه

كانت صفحة الشات لا تعرض قائمة الأزرار الأربعة المطلوبة، وكانت تبدأ برسالة ترحيب عامة وتحتوي على عبارات وتسميات تقنية لا تناسب التجربة البشرية المطلوبة. تم تعديل الصفحة بحيث تعرض باستمرار عند عدم وجود فورم نشط:

| الزر | القيمة المرسلة |
|---|---|
| 🔍 أشتري | شراء |
| 🏠 أستأجر | إيجار |
| 💰 أبيع | عرض عقار للبيع |
| 🔑 أأجر | عرض عقار للإيجار |

تم الإبقاء على `WORKER_URL` كما هو دون تعديل، ولم يتم تغيير أي موديل أو منطق اتصال. أثناء الفورم تختفي القائمة العامة حتى لا تشتت العميل، وتظهر خيارات السؤال التي يرسلها الـ Worker. بعد الإلغاء أو انتهاء الحالة غير النشطة تعود القائمة الرئيسية تلقائياً.

كما تم تغيير البداية إلى: «معاك طارق طنطاوي. اختار طلبك من الأزرار أو اكتبلي محتاج إيه في مدينة نصر.»، وحُذفت من الواجهة العبارات التي تمنح إحساساً آلياً أو لا تناسب الأسلوب المحدد.

## نتائج الاختبار الفعلي

الأزرار الأربعة بالنصوص النهائية فتحت المسارات الصحيحة:

| المسار | النتيجة الفعلية |
|---|---|
| شراء | فورم مشتري من 6 خطوات، يبدأ بنوع العقار |
| استئجار | فورم مشتري إيجار من 7 خطوات، يبدأ بنوع العقار |
| بيع | فورم مالك بيع من 11 خطوة |
| تأجير | فورم مالك إيجار من 11 خطوة |

تم اختبار مشتري شراء كامل حتى إدخال الرقم. عند عدم وجود عقار مطابق، ظهر رد واضح يذكر المنطقة والميزانية، ثم عُرض خيار تسجيل الطلب وإرساله لطارق عند الموافقة. هذا يتفق مع مسار الطلبات المعلقة.

تم اختبار خارج النطاق برسائل عن التجمع ومصر الجديدة، وتم رفضها قبل بدء الفورم. كما تم التأكد من أن إرسال «مكرم عبيد» يدخل خطوة الميزانية، وأن «المنطقة السادسة» تدخل خطوة المساحة، بما يؤكد عمل النطاق المسموح في بداية المسار.

## ما يحتاج مراجعة في مصدر الـ Worker

مصدر الـ Worker نفسه غير موجود في مستودع الموقع؛ المتاح هو endpoint منشور فقط. لذلك لا يمكن تعديل أو إثبات تفاصيل داخلية مثل ترتيب `isOutOfCoverage()` قبل كل فورم، أو أسماء حقول Gemini، من دون مستودع Worker أو صلاحية نشره.

خلال الاختبار ظهرت ملاحظة تجربة مهمة: عند إدخال إجابة نصية لا تطابق زر السؤال، يعيد النظام «اختار من الأزرار» ويحافظ على نفس الخطوة، وهذا آمن وظيفياً لكنه يحتاج رسالة مساعدة أكثر إنسانية للعميل، مثل طلب مثال مقبول، خصوصاً في الدور والتشطيب. هذه الملاحظة لا يمكن إصلاحها بأمان من صفحة الواجهة وحدها.

## تناقض يجب حسمه قبل اعتماد البيانات

المستودع المحلي يحتوي على `ai-feed.json` بإصدار `2.1`، بينما السياق المطلوب يذكر `v2.2`. كما أن الملف المحلي يعرض `priceNumeric` و`areaNumeric` فقط بصيغة نصية مشتقة من الحقول الوصفية، ولا يظهر فيه `roomsNumeric` و`bathsNumeric` كحقلين مستقلين. عدد ملفات العقارات المحلي هو 91.

لم أغيّر الـ feed تلقائياً حتى لا أكسر اعتماداً قائماً أو أكتب أرقاماً غير دقيقة من نصوص مثل «3 غرف نوم + ريسبشن». يلزم اعتماد نسخة المصدر الصحيحة أو إرسال تحديث v2.2 الفعلي قبل تعديل هذه النقطة.

## الخلاصة المهنية

العميل الآن يرى مدخلاً واضحاً، لا يحتاج إلى تخمين المسار، ولا يرى تسمية تقنية. المسارات الأربعة تستجيب، النطاق الجغرافي يعمل في بداية الطلب، ومسار عدم وجود نتائج لا يترك العميل بلا حل. الاعتماد النهائي يحتاج فقط إلى مراجعة مصدر الـ Worker ومواءمة نسخة `ai-feed.json` المحلية مع v2.2 المعلنة، ثم اختبار دورة كاملة لكل من بيع المالك، تأجير المالك، استئجار الباحث، والرجوع/الإلغاء/التعديل من خلال جلسة متصفح حقيقية.

## الملفات والاختبارات

- تعديل الواجهة: `agent.html`
- اختبار API القابل لإعادة التشغيل: `/home/ubuntu/test_worker_flows.py`
- نتائج الاختبار: `/home/ubuntu/worker_flow_results.jsonl`
- الملخص: `/home/ubuntu/worker_flow_summary.txt`
- Commit: `fd72065 Align chat UI with Semsar Talabak V24 flows`
